# Build-the-Customer-Portal
Another frontend for the same Customer CRUD API — this time the classic enterprise stack: ASP.NET Core MVC with Razor, styled with Bootstrap, talking to the API through a server-side proxy, with Knockout.js doing the MVVM binding and jQuery AJAX moving the data.
